extends _base


block header
    h1=t('step maif title')

block region
    div(class="services")

block controls
    button(class="next", aria-disabled="true", disabled="disabled")= t('step maif next')
    p(class="pass")
      a(href="#")= t('step maif pass')
